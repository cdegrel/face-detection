version: "3.8"

services:
  face-detection:
    build: .
    ports:
      - "5005:5005"
    volumes:
      - ./face_references.pkl:/app/face_references.pkl
    deploy:
      resources:
        limits:
          memory: 2G
    restart: unless-stopped
